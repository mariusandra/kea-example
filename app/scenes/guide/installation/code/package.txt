# if you're using yarn
yarn add kea
yarn add redux react-redux reselect # you will need these if not already installed

# if you're using npm
npm install kea --save
npm install redux react-redux reselect --save # you will need these if not already installed
